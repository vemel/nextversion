export enum Inputs {
    Version = "version",
    Type = "type",
    Prerelease = "prerelease",
    PrereleaseType = "prerelease-type",
    ReleaseType = "release-type",
    Result = "result",
    Path = "path"
}

export enum Outputs {
    Input = "input",
    RawInput = "raw-input",
    Major = "major",
    Minor = "minor",
    Patch = "patch",
    Micro = "micro",
    Prerelease = "prerelease",
    Result = "result"
}
